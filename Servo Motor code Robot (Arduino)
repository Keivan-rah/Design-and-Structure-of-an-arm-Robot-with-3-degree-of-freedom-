/* Sweep
 
 http://www.arduino.cc/en/Tutorial/Sweep
*/

#include <Servo.h>

Servo theta1;  // create servo object to control a servo
Servo theta2;
// twelve servo objects can be created on most boards

int pos = 180;    // variable to store the servo position

void setup() {
  theta1.attach(8);  // attaches the servo on pin 9 to the servo object
  theta2.attach(9);
}

void loop() {
  //for (pos = 0; pos <= 180; pos += 1) 
//  { // goes from 0 degrees to 180 degrees
    // in steps of 1 degree
    theta1.write(0);              // tell servo to go to position in variable 'pos'
    theta2.write(0);
    delay(1500);
      theta1.write(90);              // tell servo to go to position in variable 'pos'
    theta2.write(90);
   delay(1500);
//  }
//  for (pos = 180; pos >= 0; pos -= 1) { // goes from 180 degrees to 0 degrees
//    myservo.write(pos);              // tell servo to go to position in variable 'pos'
//    delay(15);                       // waits 15ms for the servo to reach the position
//  }
}
